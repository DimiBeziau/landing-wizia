services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: landing-wizia-build
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
    restart: unless-stopped
